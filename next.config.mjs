/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['docs.manauaradiesel.com.br'],
        unoptimized: true, 
      },
    output: "export",
    
};

export default nextConfig;
